# Copy to .env.local and fill in real values. Do NOT commit .env.local.

# ── Supabase ─────────────────────────────────────────────────────────
# From Supabase Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — SERVER ONLY. Bypasses RLS. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Direct Postgres connection (for Drizzle migrations + server-side queries that need raw SQL)
# Supabase Project Settings → Database → Connection string (use the "session" pooler for migrations)
DATABASE_URL=postgresql://postgres:PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# ── App ──────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ── Email (Office 365 SMTP) ──────────────────────────────────────────
# Used by lib/email/client.ts for all transactional email.
# Office 365 over port 587 requires STARTTLS (configured automatically).
# SMTP_PASS must be an app password if MFA is on for SMTP_USER. The mailbox
# also needs SMTP AUTH enabled (Exchange admin → mailbox → Manage email apps).
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=digital@dastifysolutions.com
SMTP_PASS=
SMTP_FROM=Dastify Credentialing <digital@dastifysolutions.com>

# ── Background jobs (Inngest) ────────────────────────────────────────
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# ── Field encryption (pgcrypto) ──────────────────────────────────────
# Used by encrypted-column helpers; rotate carefully (requires re-encrypting columns).
# Generate with: openssl rand -base64 48
PGCRYPTO_SYMMETRIC_KEY=
